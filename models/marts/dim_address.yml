version: 2
models:
  - name: int_address_customer
    description: Int de address dos clientes da Adventure Works.
    columns:
      - name: id_address
        description: Chave primária da dim_address.
        tests:
          - unique
          - not_null